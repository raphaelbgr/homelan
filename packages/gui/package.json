{
  "name": "@homelan/gui",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "echo 'gui: placeholder package, skipping build'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  }
}
