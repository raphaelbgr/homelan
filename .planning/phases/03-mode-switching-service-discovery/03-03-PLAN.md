---
phase: 03-mode-switching-service-discovery
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified: []
autonomous: false
requirements:
  - TUNL-07
  - DISC-01
  - DISC-02
  - DISC-03
  - CLI-04
  - CLI-05

must_haves:
  truths:
    - "All 6 Phase 3 requirements are verified (TUNL-07, DISC-01, DISC-02, DISC-03, CLI-04, CLI-05)"
    - "Full test suite passes with no regressions"
    - "`homelan switch-mode` and `homelan devices` are registered in CLI help output"
    - "IPC POST /switch-mode returns 200 (not 501)"
    - "IPC GET /devices returns device array (not empty)"
  artifacts:
    - path: "packages/daemon/src/platform/arp.ts"
      provides: "ARP scanner module"
    - path: "packages/daemon/src/platform/arp.test.ts"
      provides: "ARP test suite"
    - path: "packages/cli/src/commands/switchMode.ts"
      provides: "switch-mode CLI command"
    - path: "packages/cli/src/commands/devices.ts"
      provides: "devices CLI command"
  key_links:
    - from: "packages/cli/src/index.ts"
      to: "switchModeCommand + devicesCommand"
      via: "program.addCommand"
      pattern: "addCommand.*switchMode|addCommand.*devices"
---

<objective>
Phase 3 integration verification — confirm mode switching and device discovery work end-to-end.

Purpose: Gate before moving to Phase 4. Verifies all 6 Phase 3 requirements are satisfied.
Output: Passing test suite + human smoke test confirmation.
</objective>

<execution_context>
@C:/Users/rbgnr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rbgnr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-mode-switching-service-discovery/03-01-SUMMARY.md
@.planning/phases/03-mode-switching-service-discovery/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite verification</name>
  <files></files>
  <action>
Run the full monorepo test suite and build. Verify:

1. `pnpm -r build` — zero TypeScript errors across all 5 packages (shared, relay, daemon, cli, gui)
2. `pnpm -r test` — all tests pass; count must be >= 118 (Phase 2 baseline)
3. Check new test counts: arp.test.ts should have >= 6 tests, daemon.test.ts should have new switchMode and discovery tests

If any tests fail: diagnose and fix before proceeding to checkpoint. Common issues to check:
- Import paths for new arp.ts module (use .js extension in imports)
- Daemon constructor signature changes breaking existing daemon.test.ts (add new params with defaults)
- CLI index.ts missing addCommand calls
- Type errors from new methods on Daemon class

Fix inline — do not create new plan. Document any fixes in the SUMMARY.

After tests pass, also verify CLI help:
```bash
node packages/cli/dist/index.js --help
# Should show: connect, disconnect, status, switch-mode, devices
node packages/cli/dist/index.js switch-mode --help
node packages/cli/dist/index.js devices --help
```
  </action>
  <verify>
    <automated>cd C:/Users/rbgnr/git/personal-tunnel && pnpm -r build 2>&1 | tail -5 && pnpm -r test 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass (>= 118 total). Zero TypeScript errors. CLI shows 5 commands in --help output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 3 complete: mode switching (homelan switch-mode) + device discovery (homelan devices).

Automated verification:
- Full monorepo build: zero TypeScript errors
- Full test suite: all tests passing (>= 118 tests)
- CLI: 5 commands registered (connect, disconnect, status, switch-mode, devices)
- IPC: POST /switch-mode wired (not 501), GET /devices returns real data
  </what-built>
  <how-to-verify>
1. Run: `node packages/cli/dist/index.js --help`
   - Expected: shows connect, disconnect, status, switch-mode, devices

2. Run: `node packages/cli/dist/index.js switch-mode --help`
   - Expected: shows `<mode>` argument description and --json flag

3. Run: `node packages/cli/dist/index.js devices --help`
   - Expected: shows --json flag

4. Run: `node packages/cli/dist/index.js switch-mode invalid`
   - Expected: exits 1 with error "Invalid mode"

5. Run: `node packages/cli/dist/index.js devices --json`
   - Expected: exits 3 with "Error: homelan daemon is not running" (daemon not running locally)

6. Review the test summary output from Task 1 — all new tests passing?
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 3 gate — all 6 requirements verified:
- TUNL-07: switch-mode command exists and daemon.switchMode() implemented
- DISC-01: devices command shows human-readable names
- DISC-02: devices include IP, hostname, deviceType
- DISC-03: discovery auto-polls on 30s interval while connected
- CLI-04: `homelan switch-mode` command registered and functional
- CLI-05: `homelan devices` command registered with table + --json output
</verification>

<success_criteria>
- Full monorepo builds clean (zero TS errors)
- All tests pass (>= 118 baseline)
- All 5 CLI commands visible in `homelan --help`
- Human checkpoint approved
- Phase 3 marked COMPLETE in STATE.md
</success_criteria>

<output>
After completion, create `.planning/phases/03-mode-switching-service-discovery/03-03-SUMMARY.md`

Update STATE.md: change "Phase 3: Mode Switching + Discovery → 0%" to "100%", update stopped_at to "Phase 3 COMPLETE", add session notes for Phase 3.

Update ROADMAP.md progress table for Phase 3.
</output>
