---
phase: 02-tunnel-connectivity-nat-traversal
plan: 06
type: execute
wave: 4
depends_on: [02-04, 02-05]
files_modified: []
autonomous: false
requirements: [TUNL-01, TUNL-02, TUNL-03, TUNL-05, TUNL-06, TUNL-08, TUNL-09, NAT-01, NAT-02, NAT-03, NAT-04, NAT-05, CLI-01, CLI-02, CLI-03, CLI-06, CLI-07]

must_haves:
  truths:
    - "Full test suite (all packages) passes with zero failures"
    - "homelan --help shows connect, disconnect, status commands with correct options"
    - "IPC /connect and /disconnect return 200 (not 501) when daemon is running"
    - "All 17 Phase 2 requirements are addressed by prior plans"
  artifacts:
    - path: "packages/daemon/src/nat/stun.ts"
      provides: "STUN client"
    - path: "packages/daemon/src/nat/relayClient.ts"
      provides: "Relay HTTP client"
    - path: "packages/daemon/src/nat/holePunch.ts"
      provides: "UDP hole punch"
    - path: "packages/daemon/src/platform/dns.ts"
      provides: "DNS configurator"
    - path: "packages/daemon/src/platform/ipv6.ts"
      provides: "IPv6 blocker"
    - path: "packages/cli/src/index.ts"
      provides: "CLI entry point"
    - path: "packages/relay/src/routes/relay.ts"
      provides: "WebSocket relay"
  key_links:
    - from: "packages/cli/src/index.ts"
      to: "packages/daemon/src/ipc/server.ts"
      via: "HTTP calls to 127.0.0.1:30001"
      pattern: "30001"
---

<objective>
Verify the complete Phase 2 implementation: run the full monorepo test suite, confirm CLI builds and help output is correct, and manually verify the IPC layer is wired up (no 501s). This is the quality gate before Phase 3.

Purpose: Phase gate — confirms all 17 Phase 2 requirements are implemented and tested before proceeding.
Output: No new files. Phase 2 gate passed or gaps identified.
</objective>

<execution_context>
@C:/Users/rbgnr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rbgnr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-tunnel-connectivity-nat-traversal/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full monorepo test suite verification</name>
  <files></files>
  <action>
    Run the full monorepo test suite and verify all packages pass:

    ```bash
    cd /c/Users/rbgnr/git/personal-tunnel && pnpm test 2>&1
    ```

    Expected output:
    - @homelan/shared: 6 tests passing
    - @homelan/relay: 22+ tests passing (18 existing + 4 new WebSocket tests)
    - @homelan/daemon: 70+ tests passing (61 existing + new STUN/relayClient/holePunch/platform/connect tests)
    - @homelan/cli: 4 tests passing (IpcClient tests)
    - @homelan/gui: 0 tests (passWithNoTests — OK)

    If any test fails: diagnose and fix before proceeding.

    Also verify full TypeScript build:
    ```bash
    cd /c/Users/rbgnr/git/personal-tunnel && pnpm build 2>&1
    ```
    All packages must build with zero TypeScript errors.
  </action>
  <verify>
    <automated>cd /c/Users/rbgnr/git/personal-tunnel && pnpm test 2>&1 | tail -15</automated>
  </verify>
  <done>All packages test suites pass. Full monorepo build succeeds with zero TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human smoke test of CLI and IPC</name>
  <files></files>
  <action>Run manual CLI smoke tests per how-to-verify steps below. Pause for user confirmation.</action>
  <verify>User types "approved" after verifying all five checks.</verify>
  <done>User has confirmed CLI help output, exit codes, and STUN tests all appear correct.</done>
  <what-built>
    Complete Phase 2 implementation:
    - STUN client (RFC 5389 XOR-MAPPED-ADDRESS)
    - Relay HTTP client (register/lookup/auto-renew)
    - WebSocket relay endpoint on relay server (/relay)
    - UDP hole punching (direct P2P attempt)
    - Daemon.connect() + Daemon.disconnect() (full orchestration)
    - DNS configurator (netsh/networksetup per platform)
    - IPv6 blocker (netsh/networksetup per platform)
    - CLI package: homelan connect/disconnect/status with exit codes, spinner, --json flag
  </what-built>
  <how-to-verify>
    1. Verify CLI is accessible:
       ```
       node packages/cli/dist/index.js --help
       ```
       Should show: homelan [options] [command], with connect, disconnect, status listed.

    2. Verify connect options:
       ```
       node packages/cli/dist/index.js connect --help
       ```
       Should show: --mode, --timeout, --retry, --json options. Default mode: lan-only.

    3. Verify status options:
       ```
       node packages/cli/dist/index.js status --help
       ```
       Should show: --human, --json options.

    4. Verify daemon exit code when not running:
       ```
       node packages/cli/dist/index.js status; echo "Exit: $?"
       ```
       Should exit with code 3 (daemon not running), not hang.

    5. Spot-check one test file (STUN):
       ```
       pnpm --filter @homelan/daemon test -- --reporter=verbose 2>&1 | grep stun
       ```
       Should show stun tests passing.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All checks above constitute the phase gate. Automated tests are primary verification; human checkpoint confirms CLI UX quality.
</verification>

<success_criteria>
- pnpm test passes across all 5 packages with zero failures
- pnpm build succeeds with zero TypeScript errors
- homelan --help shows all three Phase 2 commands
- homelan connect --help shows --mode defaulting to lan-only
- homelan status exits with code 3 when daemon is not running (not a hang)
- Human approves verification checkpoint
</success_criteria>

<output>
After completion, create .planning/phases/02-tunnel-connectivity-nat-traversal/02-06-SUMMARY.md

Update STATE.md:
- stopped_at: "Phase 2 COMPLETE"
- completed_phases: 2
- Add session note for Phase 2 completion
- Mark all Phase 2 requirements as complete in REQUIREMENTS.md
</output>
