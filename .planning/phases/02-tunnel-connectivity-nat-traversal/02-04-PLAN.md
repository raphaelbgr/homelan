---
phase: 02-tunnel-connectivity-nat-traversal
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - packages/daemon/src/platform/dns.ts
  - packages/daemon/src/platform/dns.test.ts
  - packages/daemon/src/platform/ipv6.ts
  - packages/daemon/src/platform/ipv6.test.ts
  - packages/daemon/src/daemon.ts
autonomous: true
requirements: [TUNL-05, TUNL-06, TUNL-08, TUNL-09]

must_haves:
  truths:
    - "In Full Gateway mode, DNS resolver is set to home network DNS (e.g. 192.168.7.1) on the tunnel interface"
    - "In LAN-Only mode, client keeps its existing DNS; no DNS change is applied"
    - "IPv6 is blocked via firewall rules while tunnel is active to prevent IP leaks"
    - "DNS and IPv6 rules are reverted cleanly on disconnect"
  artifacts:
    - path: "packages/daemon/src/platform/dns.ts"
      provides: "Platform DNS configurator — setDns(iface, servers) and restoreDns()"
      exports: ["DnsConfigurator", "createDnsConfigurator"]
    - path: "packages/daemon/src/platform/ipv6.ts"
      provides: "Platform IPv6 blocker — blockIpv6() and restoreIpv6()"
      exports: ["IPv6Blocker", "createIPv6Blocker"]
  key_links:
    - from: "packages/daemon/src/daemon.ts"
      to: "packages/daemon/src/platform/dns.ts"
      via: "dnsConfigurator.setDns() in connect(), dnsConfigurator.restoreDns() in disconnect()"
      pattern: "dnsConfigurator"
    - from: "packages/daemon/src/daemon.ts"
      to: "packages/daemon/src/platform/ipv6.ts"
      via: "ipv6Blocker.blockIpv6() in connect(), ipv6Blocker.restoreIpv6() in disconnect()"
      pattern: "ipv6Blocker"
---

<objective>
Implement platform-specific DNS configuration and IPv6 blocking that the daemon applies on connect/disconnect. These handlers execute the OS-level commands (netsh on Windows, networksetup on macOS) to enforce correct routing policy for each tunnel mode.

Purpose: TUNL-05/06/08/09 — mode-aware DNS and IPv6 leak prevention.
Output: packages/daemon/src/platform/dns.ts, packages/daemon/src/platform/ipv6.ts, daemon.ts updated
</objective>

<execution_context>
@C:/Users/rbgnr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rbgnr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tunnel-connectivity-nat-traversal/02-CONTEXT.md
@.planning/phases/02-tunnel-connectivity-nat-traversal/02-03-SUMMARY.md

<interfaces>
<!-- DNS decision per CONTEXT.md (locked decisions) -->
<!-- Full Gateway: push home DNS (e.g. 192.168.7.1) as sole resolver -->
<!-- LAN-Only: keep client's existing DNS, no change -->
<!-- Platform commands (locked in CONTEXT.md):
     Windows DNS:  netsh interface ip set dns "homelan" static 192.168.7.1
     Windows restore: netsh interface ip set dns "homelan" dhcp
     macOS DNS: networksetup -setdnsservers Wi-Fi 192.168.7.1
     macOS restore: networksetup -setdnsservers Wi-Fi "empty"
     IPv6 Windows: netsh interface ipv6 set privacy state=disabled && netsh interface ipv6 delete address "homelan" ... (block all IPv6 on tunnel iface)
     IPv6 macOS: networksetup -setv6off "homelan"
-->

From packages/daemon/src/utils/execFile.ts (Phase 1):
```typescript
export function execFileSafe(cmd: string, args: string[]): Promise<{ stdout: string; stderr: string }>;
export type ShellExecutor = (cmd: string, args: string[]) => Promise<{ stdout: string; stderr: string }>;
```

From packages/daemon/src/daemon.ts (updated in 02-03):
```typescript
// connect() and disconnect() methods exist
// DnsConfigurator and IPv6Blocker will be injected via constructor for testability
```

Pattern from Phase 1: inject ShellExecutor for testability; FileKeystore was test double for OS keychain.
Same pattern here: DnsConfigurator uses injected executor, tests use vi.fn() mock executor.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DnsConfigurator + IPv6Blocker platform modules</name>
  <files>packages/daemon/src/platform/dns.ts, packages/daemon/src/platform/dns.test.ts, packages/daemon/src/platform/ipv6.ts, packages/daemon/src/platform/ipv6.test.ts</files>
  <behavior>
    DnsConfigurator:
    - createDnsConfigurator(executor?: ShellExecutor): DnsConfigurator
    - setDns(interfaceName: string, dnsServer: string): Promise<void>
      - Windows: execFileSafe("netsh", ["interface", "ip", "set", "dns", interfaceName, "static", dnsServer])
      - macOS: execFileSafe("networksetup", ["-setdnsservers", interfaceName, dnsServer])
      - Detects platform via process.platform ("win32" vs "darwin")
    - restoreDns(interfaceName: string): Promise<void>
      - Windows: execFileSafe("netsh", ["interface", "ip", "set", "dns", interfaceName, "dhcp"])
      - macOS: execFileSafe("networksetup", ["-setdnsservers", interfaceName, "empty"])

    IPv6Blocker:
    - createIPv6Blocker(executor?: ShellExecutor): IPv6Blocker
    - blockIPv6(interfaceName: string): Promise<void>
      - Windows: execFileSafe("netsh", ["interface", "ipv6", "set", "interface", interfaceName, "advertise=disabled"])
      - macOS: execFileSafe("networksetup", ["-setv6off", interfaceName])
      - Also logs warning: console.warn("[ipv6] IPv6 disabled on tunnel interface")
    - restoreIPv6(interfaceName: string): Promise<void>
      - Windows: execFileSafe("netsh", ["interface", "ipv6", "set", "interface", interfaceName, "advertise=enabled"])
      - macOS: execFileSafe("networksetup", ["-setv6automatic", interfaceName])

    Tests (use mock executor, NOT real OS calls — same pattern as Phase 1 shell tests):
    - Test 1: setDns on win32 → calls netsh with correct args
    - Test 2: restoreDns on win32 → calls netsh dhcp args
    - Test 3: setDns on darwin → calls networksetup with -setdnsservers
    - Test 4: blockIPv6 on win32 → calls netsh ipv6 interface args
    - Test 5: restoreIPv6 on darwin → calls networksetup -setv6automatic

    To test platform branches without actually being on that platform: mock process.platform by using a platform parameter instead: createDnsConfigurator({ executor, platform: "win32" | "darwin" }) — defaults to process.platform.
  </behavior>
  <action>
    Create packages/daemon/src/platform/dns.test.ts (TDD RED) with 3 DNS tests using vi.fn() mock executor.
    Create packages/daemon/src/platform/ipv6.test.ts (TDD RED) with 2 IPv6 tests.

    Create packages/daemon/src/platform/dns.ts:
    ```typescript
    export interface DnsConfigurator {
      setDns(interfaceName: string, dnsServer: string): Promise<void>;
      restoreDns(interfaceName: string): Promise<void>;
    }
    export function createDnsConfigurator(opts?: { executor?: ShellExecutor; platform?: NodeJS.Platform }): DnsConfigurator
    ```

    Create packages/daemon/src/platform/ipv6.ts similarly.

    Platform detection defaults to process.platform but can be overridden for tests.
    All shell commands use execFileSafe (no string interpolation ever).
  </action>
  <verify>
    <automated>cd /c/Users/rbgnr/git/personal-tunnel && pnpm --filter @homelan/daemon test -- --reporter=verbose 2>&1 | grep -E "(dns|ipv6|platform|PASS|FAIL|✓|✗)"</automated>
  </verify>
  <done>5 platform tests pass. DnsConfigurator calls correct netsh/networksetup commands per platform. IPv6Blocker disables/restores IPv6 per platform. No real OS calls in tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DNS + IPv6 into Daemon connect/disconnect</name>
  <files>packages/daemon/src/daemon.ts</files>
  <action>
    Update packages/daemon/src/daemon.ts to use DnsConfigurator and IPv6Blocker:

    1. Add constructor parameters (injectable for tests, default to real implementations):
       ```typescript
       private readonly dnsConfigurator: DnsConfigurator = createDnsConfigurator()
       private readonly ipv6Blocker: IPv6Blocker = createIPv6Blocker()
       ```

    2. In connect():
       - After wgInterface.up() succeeds:
         - Always: ipv6Blocker.blockIPv6("homelan")
         - If mode === "full-gateway": dnsConfigurator.setDns("homelan", "192.168.7.1")
         - If mode === "lan-only": skip DNS change (keep existing resolver per CONTEXT.md decision)
       - Wrap in try/catch — if DNS/IPv6 config fails, log warning but don't fail the connection (WireGuard tunnel is up, routing works even without DNS enforcement)

    3. In disconnect():
       - After wgInterface.down():
         - ipv6Blocker.restoreIPv6("homelan")
         - dnsConfigurator.restoreDns("homelan") (restoring in both modes is safe — no-op if not set)

    4. Update existing daemon.test.ts connect/disconnect tests to inject mock DnsConfigurator and IPv6Blocker via constructor (vi.fn() stubs). Verify setDns is called for full-gateway mode, not called for lan-only mode.

    Existing daemon tests from 02-03 must remain passing — this is additive wiring only.
  </action>
  <verify>
    <automated>cd /c/Users/rbgnr/git/personal-tunnel && pnpm --filter @homelan/daemon test 2>&1 | tail -8</automated>
  </verify>
  <done>All daemon tests pass. Full Gateway connect calls setDns. LAN-Only connect skips setDns. Both paths call blockIPv6. Disconnect calls restoreDns + restoreIPv6.</done>
</task>

</tasks>

<verification>
Full daemon test suite including all platform tests:

```bash
cd /c/Users/rbgnr/git/personal-tunnel && pnpm --filter @homelan/daemon test 2>&1 | tail -5
```

TypeScript strict mode, zero errors:

```bash
cd /c/Users/rbgnr/git/personal-tunnel && pnpm --filter @homelan/daemon typecheck 2>&1
```
</verification>

<success_criteria>
- DNS set to home DNS (192.168.7.1) in Full Gateway mode; unchanged in LAN-Only mode
- IPv6 blocked on tunnel interface in both modes
- All OS commands use execFileSafe (no string interpolation)
- Platform detection is mockable (no test makes real OS calls)
- All daemon tests pass (prior 61 + new platform tests)
</success_criteria>

<output>
After completion, create .planning/phases/02-tunnel-connectivity-nat-traversal/02-04-SUMMARY.md
</output>
