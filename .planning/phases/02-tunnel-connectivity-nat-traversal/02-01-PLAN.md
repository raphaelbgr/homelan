---
phase: 02-tunnel-connectivity-nat-traversal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/nat/stun.ts
  - packages/daemon/src/nat/stun.test.ts
  - packages/daemon/src/nat/relayClient.ts
  - packages/daemon/src/nat/relayClient.test.ts
  - packages/shared/src/types/nat.ts
  - packages/shared/src/index.ts
autonomous: true
requirements: [NAT-01, NAT-02, NAT-04, NAT-05]

must_haves:
  truths:
    - "Daemon can discover its external IP:port via STUN without any binary dependency"
    - "Daemon can register itself with the relay server and look up a peer's endpoint"
    - "NAT type contracts (StunResult, RelayRegistration) are defined in shared and exported"
  artifacts:
    - path: "packages/daemon/src/nat/stun.ts"
      provides: "STUN UDP client — resolveExternalEndpoint(stunServer)"
      exports: ["resolveExternalEndpoint", "StunResult"]
    - path: "packages/daemon/src/nat/relayClient.ts"
      provides: "Relay HTTP client — register, lookup, re-registration interval"
      exports: ["RelayClient"]
    - path: "packages/shared/src/types/nat.ts"
      provides: "NAT traversal shared types"
      exports: ["StunResult", "ConnectionProgress", "NatTraversalConfig"]
  key_links:
    - from: "packages/daemon/src/nat/relayClient.ts"
      to: "packages/relay/src/routes/register.ts"
      via: "POST /register with {publicKey, endpoint, timestampMs}"
      pattern: "fetch.*register"
    - from: "packages/daemon/src/nat/stun.ts"
      to: "stun.l.google.com:19302"
      via: "raw UDP STUN binding request"
      pattern: "dgram.*createSocket"
---

<objective>
Build the NAT discovery layer: a pure-Node.js STUN client for external endpoint discovery, and a relay HTTP client for peer registration/lookup. These two modules together enable the daemon to answer "where am I on the internet?" and "where is my peer?".

Purpose: Daemon.connect() (plan 02-03) needs these to be solid, tested modules before orchestrating a connection attempt.
Output: packages/daemon/src/nat/stun.ts, packages/daemon/src/nat/relayClient.ts, packages/shared/src/types/nat.ts
</objective>

<execution_context>
@C:/Users/rbgnr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rbgnr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-tunnel-connectivity-nat-traversal/02-CONTEXT.md

<interfaces>
<!-- Key types from Phase 1 that this plan builds against -->

From packages/shared/src/types/relay.ts:
```typescript
export interface RegisterRequest {
  publicKey: string;   // WireGuard public key (base64, 44 chars)
  endpoint: string;    // "ip:port" or "" if behind NAT
  timestampMs: number; // Unix ms for TTL-based expiry
}
export interface RegisterResponse { ok: boolean; ttlSeconds: number; }
export interface LookupResponse { publicKey: string; endpoint: string; timestampMs: number; }
```

From packages/daemon/src/utils/execFile.ts:
```typescript
// Note: STUN and relay client must NOT use execFileSafe — pure Node.js only
export type ShellExecutor = (cmd: string, args: string[]) => Promise<{ stdout: string; stderr: string }>;
```

Established pattern: all tests use vitest, TDD (write failing test first), no external binary deps.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared NAT types + STUN client</name>
  <files>packages/shared/src/types/nat.ts, packages/shared/src/index.ts, packages/daemon/src/nat/stun.ts, packages/daemon/src/nat/stun.test.ts</files>
  <behavior>
    - StunResult: { ip: string; port: number; family: "IPv4" }
    - ConnectionProgress: union type "discovering_peer" | "trying_direct" | "trying_relay" | "connected" | "error"
    - NatTraversalConfig: { stunServers: string[]; holePunchTimeoutMs: number; relayUrl: string; relaySecret: string; peerPublicKey: string }
    - resolveExternalEndpoint(stunServer: string, timeoutMs?: number): Promise<StunResult> — sends a STUN Binding Request (RFC 5389) over UDP using node:dgram, parses the XOR-MAPPED-ADDRESS attribute from the response, returns { ip, port, family }
    - Throws StunError if no response within timeoutMs (default 3000ms)
    - Test 1: resolveExternalEndpoint with a mock UDP server that returns a valid STUN response → returns { ip: "1.2.3.4", port: 54321, family: "IPv4" }
    - Test 2: resolveExternalEndpoint times out → throws StunError
    - Test 3: STUN response with XOR-MAPPED-ADDRESS IPv4 correctly un-XORs the address (XOR with magic cookie 0x2112A442)
  </behavior>
  <action>
    1. Create packages/shared/src/types/nat.ts — export StunResult, ConnectionProgress, NatTraversalConfig types. Re-export from packages/shared/src/index.ts.

    2. Create packages/daemon/src/nat/stun.test.ts with failing tests first (TDD RED phase): mock a dgram socket server in-process, send fake STUN Binding Response with XOR-MAPPED-ADDRESS IPv4 attribute, assert resolveExternalEndpoint resolves correctly. Second test: use a socket that never responds, assert StunError thrown after timeout.

    3. Create packages/daemon/src/nat/stun.ts — implement resolveExternalEndpoint using node:dgram (no external deps):
       - Build STUN Binding Request: 20-byte header (type 0x0001, length 0x0000, magic cookie 0x2112A442, 12-byte transaction ID random)
       - Send to host:port (parse stunServer "host:port" format)
       - Parse response: read message type (0x0101 = Binding Response), iterate TLV attributes, find XOR-MAPPED-ADDRESS (0x0020), un-XOR IP (each byte XOR with magic cookie bytes), un-XOR port (XOR with 0x2112)
       - On timeout: reject with StunError("STUN request timed out")
       - Export: resolveExternalEndpoint, StunResult, StunError

    No external STUN library — raw dgram only (per Claude's Discretion in CONTEXT.md).
  </action>
  <verify>
    <automated>cd /c/Users/rbgnr/git/personal-tunnel && pnpm --filter @homelan/daemon test -- --reporter=verbose 2>&1 | grep -E "(stun|PASS|FAIL|✓|✗)"</automated>
  </verify>
  <done>All 3 STUN tests pass. resolveExternalEndpoint correctly parses XOR-MAPPED-ADDRESS. StunError thrown on timeout. Types exported from shared.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Relay HTTP client</name>
  <files>packages/daemon/src/nat/relayClient.ts, packages/daemon/src/nat/relayClient.test.ts</files>
  <behavior>
    - RelayClient class: constructor({ relayUrl, relaySecret, publicKey })
    - register(endpoint: string): Promise<void> — POST /register with {publicKey, endpoint, timestampMs: Date.now()}, Authorization: Bearer {relaySecret}. Throws RelayClientError on non-200.
    - lookup(peerPublicKey: string): Promise<LookupResponse> — GET /lookup/:peerPublicKey. Throws RelayClientError with code "NOT_FOUND" if 404.
    - startAutoRenew(ttlSeconds: number): () => void — calls register() every ttlSeconds/2 * 1000 ms. Returns stop function.
    - Test 1: register() sends correct POST body and Authorization header
    - Test 2: lookup() returns LookupResponse on 200
    - Test 3: lookup() throws RelayClientError with code NOT_FOUND on 404
    - Test 4: startAutoRenew() calls register() at correct interval (use fake timers)
  </behavior>
  <action>
    Create packages/daemon/src/nat/relayClient.test.ts first (TDD RED): use vitest's vi.spyOn(global, 'fetch') to mock HTTP calls. Test all four behaviors above.

    Create packages/daemon/src/nat/relayClient.ts:
    - Use native fetch (Node.js 22 built-in — no node-fetch needed)
    - RelayClientError extends Error with code: string field
    - register(): POST `${relayUrl}/register` with JSON body, Content-Type: application/json, Authorization: Bearer header
    - lookup(): GET `${relayUrl}/lookup/${peerPublicKey}`, throws RelayClientError("NOT_FOUND") if 404
    - startAutoRenew(): setInterval at ttlSeconds/2 * 1000, returns clearInterval wrapper. Log errors from auto-renew but do not throw (daemon keeps running).
    - Import LookupResponse from @homelan/shared
  </action>
  <verify>
    <automated>cd /c/Users/rbgnr/git/personal-tunnel && pnpm --filter @homelan/daemon test -- --reporter=verbose 2>&1 | grep -E "(relayClient|PASS|FAIL|✓|✗)"</automated>
  </verify>
  <done>All 4 relay client tests pass. register() and lookup() use fetch correctly with Authorization header. Auto-renew calls register at half-TTL interval.</done>
</task>

</tasks>

<verification>
Full daemon test suite still passing after additions:

```bash
cd /c/Users/rbgnr/git/personal-tunnel && pnpm --filter @homelan/daemon test 2>&1 | tail -5
```

Shared package builds without errors:

```bash
cd /c/Users/rbgnr/git/personal-tunnel && pnpm --filter @homelan/shared build 2>&1
```
</verification>

<success_criteria>
- StunResult, ConnectionProgress, NatTraversalConfig exported from @homelan/shared
- resolveExternalEndpoint correctly implements RFC 5389 XOR-MAPPED-ADDRESS parsing
- RelayClient registers/looks up peers via relay HTTP API
- All new tests pass; existing 61 daemon tests still pass
- No external npm dependencies added (pure Node.js built-ins only)
</success_criteria>

<output>
After completion, create .planning/phases/02-tunnel-connectivity-nat-traversal/02-01-SUMMARY.md
</output>
