---
phase: 02-tunnel-connectivity-nat-traversal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/relay/src/routes/relay.ts
  - packages/relay/src/routes/relay.test.ts
  - packages/relay/src/app.ts
  - packages/relay/package.json
autonomous: true
requirements: [NAT-03]

must_haves:
  truths:
    - "Relay server accepts WebSocket connections at /relay and proxies WireGuard UDP frames between two peers"
    - "Each peer identifies itself with publicKey + relaySecret when connecting; relay validates before proxying"
    - "Relay only pairs peers with matching session tokens; no cross-peer data leakage"
  artifacts:
    - path: "packages/relay/src/routes/relay.ts"
      provides: "WebSocket relay endpoint using ws library"
      exports: ["relayRouter"]
    - path: "packages/relay/src/app.ts"
      provides: "Updated Express app that mounts /relay WebSocket upgrade"
      contains: "relay"
  key_links:
    - from: "packages/relay/src/app.ts"
      to: "packages/relay/src/routes/relay.ts"
      via: "server.on('upgrade', handler)"
      pattern: "upgrade.*relay"
---

<objective>
Add a WebSocket UDP relay endpoint to the relay server. When hole punching fails (plan 02-03 decides this), the daemon connects both peers through this WebSocket relay as a fallback — the relay proxies raw WireGuard UDP frames bidirectionally.

Purpose: NAT-03 relay fallback requires the relay to act as a data proxy, not just a lookup registry.
Output: packages/relay/src/routes/relay.ts, updated packages/relay/src/app.ts
</objective>

<execution_context>
@C:/Users/rbgnr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rbgnr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tunnel-connectivity-nat-traversal/02-CONTEXT.md

<interfaces>
<!-- Existing relay app structure to extend -->

From packages/relay/src/app.ts:
```typescript
// Current structure: Express app with /register, /lookup, /health routes
// We need to add WebSocket upgrade handling to the HTTP server
// Express app is created in createApp() and http.Server created in index.ts
```

From packages/relay/src/config.ts:
```typescript
export interface RelayConfig {
  relaySecret: string;
  ttlSeconds: number;
  httpsOnly: boolean;
}
```

Established pattern in relay: route factories return IRouter, tested via supertest.
WebSocket library: install ws@^8 (battle-tested, used by most Node.js WS implementations).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WebSocket relay route + ws dependency</name>
  <files>packages/relay/package.json, packages/relay/src/routes/relay.ts, packages/relay/src/routes/relay.test.ts</files>
  <behavior>
    - Add ws@^8 + @types/ws to relay devDependencies (ws as a runtime dep too)
    - relayRouter(config: RelayConfig): { handleUpgrade(req, socket, head): void } — handles WebSocket upgrade at /relay
    - Connection protocol: client sends JSON handshake frame first: { publicKey: string, relaySecret: string, sessionToken: string }
    - Relay validates relaySecret matches config.relaySecret; closes socket with code 4001 if invalid
    - sessionToken is a shared string both peers must use to find each other (e.g., derived from sorted pair of public keys)
    - When two peers with same sessionToken connect, relay pairs them and proxies binary frames between them
    - If a peer disconnects, relay closes the other peer's connection
    - Test 1: Invalid relaySecret → connection closed with code 4001
    - Test 2: Two peers with same sessionToken → binary message from peer A received by peer B
    - Test 3: Peer A disconnects → peer B connection is closed
    - Test 4: Only one peer connected (no pair yet) → messages buffered for up to 10 seconds then dropped (peer closed)
  </behavior>
  <action>
    1. Add to packages/relay/package.json dependencies: "ws": "^8.17.0" and devDependencies: "@types/ws": "^8.5".

    2. Create packages/relay/src/routes/relay.test.ts (TDD RED): use ws client to connect to a test http.Server. Test all four behaviors above.

    3. Create packages/relay/src/routes/relay.ts:
       - Import WebSocketServer from 'ws'
       - createRelayHandler(config: RelayConfig): (req, socket, head) => void
         - Creates a WebSocketServer with { noServer: true }
         - On upgrade, calls wss.handleUpgrade → onConnection
         - onConnection: wait for first message (JSON handshake), validate relaySecret, extract sessionToken
         - Use a Map<sessionToken, WebSocket> to track waiting peers
         - When second peer arrives with same sessionToken: pipe messages bidirectionally (ws.on('message') → otherWs.send())
         - On close/error: close partner socket
       - Export: createRelayHandler

    Framing: pass raw binary buffers between peers — no re-encoding. WireGuard UDP frames are binary.
  </action>
  <verify>
    <automated>cd /c/Users/rbgnr/git/personal-tunnel && pnpm install && pnpm --filter @homelan/relay test -- --reporter=verbose 2>&1 | grep -E "(relay|PASS|FAIL|✓|✗)"</automated>
  </verify>
  <done>All 4 relay WebSocket tests pass. Two peers with matching sessionToken exchange binary messages. Invalid auth closes connection. Disconnection propagates to partner.</done>
</task>

<task type="auto">
  <name>Task 2: Mount relay handler on HTTP server</name>
  <files>packages/relay/src/app.ts, packages/relay/src/index.ts</files>
  <action>
    Update packages/relay/src/app.ts to export createRelayHandler factory alongside createApp.

    Update packages/relay/src/index.ts:
    - Import createRelayHandler from routes/relay.ts
    - After `const server = http.createServer(app)` (or wrap app in http.createServer if not already), add:
      ```typescript
      const relayHandler = createRelayHandler(config);
      server.on("upgrade", (req, socket, head) => {
        if (req.url === "/relay") {
          relayHandler(req, socket, head);
        } else {
          socket.destroy();
        }
      });
      ```
    - Ensure server.listen() is used (not app.listen()) so upgrade events are routed correctly

    If index.ts currently uses app.listen(), refactor to:
    ```typescript
    const server = http.createServer(app);
    server.on("upgrade", ...);
    server.listen(port, () => ...);
    ```

    No test needed here — the wire-up is covered by the relay.test.ts WebSocket tests (which test against a real http.Server).
  </action>
  <verify>
    <automated>cd /c/Users/rbgnr/git/personal-tunnel && pnpm --filter @homelan/relay build 2>&1 && echo "BUILD OK"</automated>
  </verify>
  <done>Relay package builds with zero TypeScript errors. /relay WebSocket upgrade path is mounted in index.ts. All existing relay tests (18) still pass.</done>
</task>

</tasks>

<verification>
Full relay test suite passes after WebSocket addition:

```bash
cd /c/Users/rbgnr/git/personal-tunnel && pnpm --filter @homelan/relay test 2>&1 | tail -5
```
</verification>

<success_criteria>
- ws@^8 added to relay package
- createRelayHandler validates relaySecret, pairs peers by sessionToken, proxies binary frames
- /relay WebSocket upgrade mounted in index.ts
- All relay tests pass (18 existing + 4 new WebSocket tests)
- Relay TypeScript build succeeds with zero errors
</success_criteria>

<output>
After completion, create .planning/phases/02-tunnel-connectivity-nat-traversal/02-02-SUMMARY.md
</output>
