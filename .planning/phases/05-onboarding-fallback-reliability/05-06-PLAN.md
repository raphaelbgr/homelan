---
phase: 05-onboarding-fallback-reliability
plan: "06"
type: execute
wave: 4
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
  - "05-04"
  - "05-05"
files_modified: []
autonomous: false
requirements:
  - AUTH-02
  - AUTH-04
  - CLDE-01
  - CLDE-02
  - CLDE-03
  - CLDE-04

must_haves:
  truths:
    - "Full monorepo builds with zero TypeScript errors"
    - "All tests pass (162 prior + new tests from phase 5)"
    - "homelan --help shows pair and history commands"
    - "Relay server exposes /invite and /pair routes"
    - "Skill file exists at .claude/skills/homelan/SKILL.md"
    - "OnboardingWizard component exists in GUI"
  artifacts:
    - path: "packages/relay/src/routes/invite.ts"
      provides: "POST /invite route"
    - path: "packages/relay/src/routes/pair.ts"
      provides: "POST /pair route"
    - path: "packages/daemon/src/history/logger.ts"
      provides: "HistoryLogger"
    - path: "packages/cli/src/commands/pair.ts"
      provides: "homelan pair command"
    - path: "packages/cli/src/commands/history.ts"
      provides: "homelan history command"
    - path: "packages/gui/src/components/OnboardingWizard.tsx"
      provides: "Onboarding wizard"
    - path: ".claude/skills/homelan/SKILL.md"
      provides: "Claude Code skill"
  key_links:
    - from: ".claude/skills/homelan/SKILL.md"
      to: "homelan CLI"
      via: "All 7 commands documented with correct syntax and exit codes"
---

<objective>
Phase 5 integration verification: confirm all requirements satisfied, full test suite passes, CLI smoke test, and human verification of GUI onboarding wizard and skill file.

Purpose: Gate ensuring Phase 5 is complete before marking the milestone done.
Output: Phase 5 verification complete, milestone v1.0 achieved.
</objective>

<execution_context>
@C:/Users/rbgnr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rbgnr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-onboarding-fallback-reliability/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite and build verification</name>
  <files></files>
  <action>
    Run the full monorepo verification sequence:

    1. Full TypeScript build across all packages:
       ```bash
       cd /c/Users/rbgnr/git/personal-tunnel && pnpm -r build
       ```
       Expect: zero errors across shared, relay, daemon, cli, gui

    2. Full test suite:
       ```bash
       pnpm -r test
       ```
       Expect: all tests pass (162 prior + new phase 5 tests from plans 01-03)

    3. CLI smoke test:
       ```bash
       node packages/cli/dist/index.js --help
       ```
       Verify: connect, disconnect, status, switch-mode, devices, pair, history all listed

    4. Relay route verification:
       ```bash
       # Check routes registered in app.ts
       grep -n "invite\|pair" packages/relay/src/app.ts
       ```
       Expect: /invite and /pair routes registered

    5. Skill file verification:
       ```bash
       ls .claude/skills/homelan/SKILL.md .claude/skills/homelan/rules/commands.md
       ```
       Expect: both files exist

    6. History logger existence:
       ```bash
       ls packages/daemon/src/history/logger.ts
       ```
       Expect: file exists

    Fix any failures before proceeding to checkpoint.
  </action>
  <verify>
    <automated>cd /c/Users/rbgnr/git/personal-tunnel && pnpm -r build 2>&1 | tail -5 && pnpm -r test 2>&1 | tail -10</automated>
  </verify>
  <done>Zero build errors; all tests pass; pair and history appear in homelan --help</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 5 complete: onboarding flow (invite/pair relay endpoints + daemon pairing + CLI pair command + GUI wizard), DDNS fallback in connect(), connection history (daemon logging + CLI history command), and Claude Code skill definition.
  </what-built>
  <how-to-verify>
    1. GUI check: Run `pnpm --filter @homelan/gui tauri dev` (or check build output). Open the app and verify a "Pair Device" button appears in the header when disconnected. Click it to verify the OnboardingWizard modal opens with an invite URL input field.

    2. CLI pair command: Run `homelan pair --help` and verify the command appears with correct argument syntax.

    3. CLI history command: Run `homelan history --help` and verify --json and --limit options appear.

    4. Relay routes: Start relay locally and verify `curl -X POST http://localhost:3000/invite -H "Authorization: Bearer test"` returns a valid invite URL response.

    5. Skill file: Open `.claude/skills/homelan/SKILL.md` and verify it documents all 7 commands with exit codes and JSON schemas.

    Approve if all 5 checks pass.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 5 success criteria from ROADMAP.md:
1. New user can onboard via QR/invite URL — relay /invite + daemon /pair + CLI pair + GUI wizard all implemented
2. GUI first-time setup wizard — OnboardingWizard component in App.tsx
3. DDNS fallback when relay unavailable — daemon connect() with trying_ddns step
4. Claude Code skill can invoke all CLI commands — SKILL.md + rules/commands.md complete
5. Connection history logged locally — HistoryLogger + homelan history command
</verification>

<success_criteria>
- AUTH-02: relay /invite + /pair + daemon pair() + CLI pair command + GUI wizard all working
- AUTH-04: OnboardingWizard component in GUI with 2-step flow
- CLDE-01..04: SKILL.md with full command docs and workflow examples
- DDNS fallback: trying_ddns progress step in ConnectionProgress union
- History: HistoryLogger + GET /history IPC + homelan history CLI command
- All 49 v1 requirements complete — milestone v1.0 achieved
</success_criteria>

<output>
After completion, create `.planning/phases/05-onboarding-fallback-reliability/05-06-SUMMARY.md`

Update ROADMAP.md Phase 5 entry to mark complete.
Update STATE.md to reflect milestone v1.0 complete.
</output>
