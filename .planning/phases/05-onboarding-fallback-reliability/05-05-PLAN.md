---
phase: 05-onboarding-fallback-reliability
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/homelan/SKILL.md
  - .claude/skills/homelan/rules/commands.md
autonomous: true
requirements:
  - CLDE-01
  - CLDE-02
  - CLDE-03
  - CLDE-04

must_haves:
  truths:
    - "Claude Code skill file exists at .claude/skills/homelan/SKILL.md"
    - "Skill describes all 7 CLI commands with exact syntax, options, and exit codes"
    - "Skill documents JSON output formats for status and devices commands"
    - "Skill documents daemon-not-running detection (exit code 3)"
    - "Skill explains when to use each command and how to interpret output"
    - "rules/commands.md contains full command reference with examples"
  artifacts:
    - path: ".claude/skills/homelan/SKILL.md"
      provides: "Claude Code skill index for HomeLAN"
    - path: ".claude/skills/homelan/rules/commands.md"
      provides: "Full CLI command reference with examples and output schemas"
  key_links:
    - from: ".claude/skills/homelan/SKILL.md"
      to: ".claude/skills/homelan/rules/commands.md"
      via: "@.claude/skills/homelan/rules/commands.md reference in SKILL.md"
---

<objective>
Create the Claude Code skill definition for HomeLAN CLI, enabling AI agents to programmatically control the VPN tunnel, check status, query devices, and manage connections.

Purpose: CLDE-01..04 — Claude Code (Raphael's primary use case) needs structured knowledge of the HomeLAN CLI to automate home LAN access. This is a documentation plan, not a code plan.
Output: SKILL.md and rules/commands.md in the project's .claude/skills/homelan/ directory.
</objective>

<execution_context>
@C:/Users/rbgnr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rbgnr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-onboarding-fallback-reliability/05-CONTEXT.md

<interfaces>
<!-- Full CLI reference for skill documentation -->

homelan connect [--mode full-gateway|lan-only] [--timeout <sec>] [--retry <n>] [--json]
  Exit: 0=connected, 1=failed, 3=daemon not running

homelan disconnect [--json]
  Exit: 0=disconnected, 1=failed, 3=daemon not running

homelan status [--human]
  Default: JSON to stdout (no flag needed)
  JSON shape: { state, mode, latencyMs, uptimeMs, lanDevices[] }
  state values: "idle" | "connecting" | "connected" | "disconnecting" | "error"
  Exit: 0=ok, 3=daemon not running

homelan switch-mode <full-gateway|lan-only> [--json]
  Exit: 0=switched, 1=failed (not connected), 3=daemon not running

homelan devices [--json]
  JSON: array of { ip, hostname, deviceType }
  Exit: 0=ok, 3=daemon not running

homelan pair <invite-url>
  Argument: homelan://pair?token=<token>&relay=<url>
  Exit: 0=paired, 1=failed, 3=daemon not running

homelan history [--json] [--limit <n>]
  JSON: array of { timestamp, action, mode?, duration_ms?, peer_endpoint?, fallback_method?, error? }
  action values: "connect" | "disconnect" | "mode_switch" | "error"
  Exit: 0=ok, 3=daemon not running

Daemon IPC: localhost:30001 (auto-started by homelan connect if not running)
Status check pattern: homelan status --json → parse state field
Connection check: exit code 3 = daemon not running → cannot proceed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Claude Code skill for HomeLAN</name>
  <files>.claude/skills/homelan/SKILL.md, .claude/skills/homelan/rules/commands.md</files>
  <action>
    Create .claude/skills/homelan/ directory.

    Create .claude/skills/homelan/SKILL.md following the transcribe-audio SKILL.md structure:

    ```markdown
    # HomeLAN

    Personal VPN tunnel tool for accessing home LAN resources from anywhere via WireGuard.
    Controls the tunnel, checks connection status, manages device discovery, and handles onboarding.

    ## When to Use

    Use this skill whenever you need to:
    - Check if the home tunnel is connected before accessing LAN resources
    - Connect to or disconnect from the home network
    - Switch between Full Gateway (all traffic) and LAN-Only (LAN traffic only) modes
    - List devices on the home network (file shares, SSH servers, local APIs)
    - Pair a new device with the home server
    - Review connection history for debugging

    ## Prerequisites

    The homelan daemon must be running. Check with:
    ```bash
    homelan status --json
    ```
    Exit code 3 = daemon not running. If not running, the user must start it (daemon lifecycle is not managed by Claude).

    ## Quick Reference

    @.claude/skills/homelan/rules/commands.md

    ## Status Detection Pattern

    Always check connection state before accessing LAN resources:
    ```bash
    STATUS=$(homelan status --json 2>/dev/null)
    EXIT=$?
    if [ $EXIT -eq 3 ]; then echo "Daemon not running"; exit 1; fi
    STATE=$(echo "$STATUS" | jq -r '.state')
    if [ "$STATE" != "connected" ]; then homelan connect; fi
    ```

    ## Error Handling

    All commands return structured exit codes:
    - 0: success
    - 1: operation failed (check stderr for details)
    - 3: daemon not running

    For --json commands, parse stdout for structured error details.
    ```

    Create .claude/skills/homelan/rules/commands.md with full command reference:

    Document each command with:
    - Exact syntax
    - All options
    - Exit codes
    - JSON output schema (for commands with --json or JSON-default output)
    - Usage example
    - Common failure modes

    Commands to document: connect, disconnect, status, switch-mode, devices, pair, history

    Include a "Workflow Examples" section with:
    1. "Connect and access a file share": connect → check state → smb access
    2. "Check network topology before SSH": devices → find IP → ssh user@IP
    3. "Switch to Full Gateway for internet routing": status → if connected → switch-mode full-gateway
    4. "Onboard a new machine": pair <url> → verify status
    5. "Debug a failed connection": history --json --limit 5 → inspect last entries
  </action>
  <verify>
    <automated>ls /c/Users/rbgnr/git/personal-tunnel/.claude/skills/homelan/SKILL.md && ls /c/Users/rbgnr/git/personal-tunnel/.claude/skills/homelan/rules/commands.md && echo "Skill files exist"</automated>
  </verify>
  <done>SKILL.md and rules/commands.md exist with complete command documentation, JSON schemas, exit codes, and workflow examples</done>
</task>

</tasks>

<verification>
Read both files and verify:
- SKILL.md has "When to Use", Prerequisites, Quick Reference, Status Detection Pattern, Error Handling sections
- commands.md documents all 7 commands with syntax, options, exit codes, and JSON schemas
- Workflow examples cover the 5 key use cases
</verification>

<success_criteria>
- .claude/skills/homelan/SKILL.md exists and covers CLDE-01..04 requirements
- rules/commands.md documents all CLI commands with exact syntax and JSON output schemas
- Skill explains daemon prerequisite and exit code 3 detection
- 5 workflow examples demonstrate common AI agent use cases
- No TypeScript changes required — documentation only
</success_criteria>

<output>
After completion, create `.planning/phases/05-onboarding-fallback-reliability/05-05-SUMMARY.md`
</output>
